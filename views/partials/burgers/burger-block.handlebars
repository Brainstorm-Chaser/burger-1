<li>
	<h3>{{burger_name}}</h3>

	<button class="btn btn-md btn-primary change-devoured" data-id="{{id}}" data-newdevour="{{devoured}}">
		{{#if devoured}}Devour!{{else}}Undevour!{{/if}}
	</button>
</li>
